# M0 Baseline Audit

状态：历史基线记录，不能单独作为七模块签收。本文记录固定基线和已执行
owner 证据；当前 G0-G5 状态以 `ACCEPTANCE_REQUIREMENTS.md` 为准。

## 固定版本

| 项目 | B0 | 冻结 N0 | C 当前候选 |
| --- | --- | --- | --- |
| PilotDeck | `ecedc5c32f2b8c8e5387cb2faba70cccf65650fd` (`origin/main`) | AgentLoop, Skill, Tool, Context/Compaction and Model Provider N0 frozen at `4355463dca4e6767b0267ff4f9fee1b0b7337cea` (parent `148a7bd6c`). Earlier AgentLoop probes at `e55b0a…`, `fb8e983…`, and `d101edf…` retain observable B0 drift and are not N0. | `ec34a7cea8c83bb76842a9cd062fa53d5fcfb31a` + current uncommitted changes |
| StaffDeck | `7adc7c84f61bd6cca13ff0380a811cbb3ae3c544` (`origin/main`) | SOP and Knowledge native owner N0 frozen at non-C `71aaede2d560450015fffb0e48bd2bcab7befc37`, retained by `codex/staffdeck-n0-owner-20260920` in `/tmp/staffdeck-n0-owner.20260920`. It is C's direct parent; `71aaede2..7fdc8d42` adds only `portable_sop/`, leaving the original owner files unchanged. | `7fdc8d427790229541405abf10fd929189b4389c` + current uncommitted changes |

B0 worktree：`/tmp/pilotdeck-staffdeck-m0.j4voeS/pilotdeck-b0`、`/tmp/pilotdeck-staffdeck-m0.j4voeS/staffdeck-b0`。B0 产品源码未打补丁；PilotDeck B0 测试只临时复用候选的 `node_modules` 符号链接。

`e55b0…` is an ancestor of the current PilotDeck worktree. Its passing
architecture-boundary test establishes import direction only. It must not be
reported as N0 behavior parity, and current C must not be reported as N0.

## 架构基准检查

解耦架构固定提交：`e55b0a82d07ee3951e5812c34103400dfa6043f7`
（`Kaguya-19/refactor/core_agent_loop_0831`，`refactor: complete sidecar composition decoupling`）。
当前候选的边界检查固定为：AgentLoop、AgentTurnCapabilities 和
`src/agent/modules` 不反向导入 `composition`、StaffDeck/SOP 或具体
Skill/Knowledge owner；`src/agent/session/AgentSessionRuntimeBundle.ts`
负责选择并组合 Ports；`SopAgentLoop` 只作为外层 decorator 接收 runner
和 sidecar modules。执行命令：

```text
PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH ./node_modules/.bin/tsc -p tsconfig.json
node --test --test-force-exit dist/tests/composition/architecture-boundary.spec.js
```

结果：`2/2 PASS`。这是依赖方向和宿主职责的机械架构证据，不替代
B0 → N0 → C 行为对照。

## 已确认 owner

PilotDeck B0 已确认：

- AgentLoop：`src/agent/loop/AgentLoop.ts`
- Skill：`src/extension/skills/SkillManager.ts`、`src/tool/builtin/readSkill.ts`
- Context/Compaction：`src/context/DefaultContextRuntime.ts`、`src/context/compaction/CompactionEngine.ts`
- Model Provider：`src/model/` 与 `ModelInvokerPort`
- Tool：`src/tool/`

StaffDeck B0 已确认：

- SOP nesting/lifecycle：`backend/app/skills/nesting.py`、`backend/tests/test_sop_nesting.py`、`backend/tests/test_nested_sop_execution.py`
- Knowledge owner：`backend/app/knowledge/service.py`、`backend/app/capabilities/local_knowledge.py`、`backend/app/api/knowledge.py`、`backend/app/api/knowledge_bases.py`

## G0 Owner Evidence Matrix

This inventory records current evidence rather than claiming G0 completion.
`B0 -> N0` and `N0 -> C` are marked missing whenever no independent frozen N0
owner exists. A B0/C runner remains useful evidence for the named surface, but
does not fill that missing three-layer comparison.

| Slot | B0 owner/entry | B0 -> N0 | N0 -> C | B0 -> C / C evidence | Remaining G0 gap |
| --- | --- | --- | --- | --- | --- |
| `agentLoop` | `src/agent/loop/AgentLoop.ts` | PASS: frozen `4355463d`, 7/7 execution differential | PASS: `4355463d` -> C, 7/7 execution differential | `B0-C-AGENTLOOP-DIFF`: execution and durable-session replay slices | Full loop matrix |
| `skills` | `SkillManager.ts`, `readSkill.ts` | PASS: frozen `4355463d`, 15/15 management/discovery differential | PASS: `4355463d` -> C, same 15 cases | `B0-C-SKILL-DIFF`: 15/15 management/discovery cases; native Gateway enabled/disabled slice | Complete session visibility/error matrix |
| `tools` | `src/tool/` | PASS: frozen `4355463d`, 23/23 registry/runtime/scheduler/side-effect/Gateway differential | PASS: `4355463d` -> C, same 23 cases | Registry/runtime/scheduler, side-effect, and Gateway restart/cancel runners | Broader owner matrix |
| `context` / Compaction | `DefaultContextRuntime.ts`, `CompactionEngine.ts` | PASS: frozen `4355463d`, Context 4/4 plus compaction 3/3, auto 5/5 and restart 3/3 | PASS: `4355463d` -> C, same 15 cases | Context/compaction differentials plus native Gateway post-restart strict replay | Closed for the declared CTX-01/CMP-01/CMP-02 cases; the current ledger's only open owner/model comparisons are recorded in `ACCEPTANCE_REQUIREMENTS.md` |
| `modelProvider` | `src/model/`, `ModelInvokerPort` | PASS: frozen `4355463d`, registry 4/4, request 4/4 and runtime 6/6 | PASS: `4355463d` -> C, same 14 cases | Provider/request differentials and streaming retry slices | Real-provider usage/retry matrix |
| `sop` | B0 StaffDeck lifecycle/nesting owner tests | PASS: B0 -> frozen `71aaede2`, GraphRules 3/3 and TurnFinalizer 5/5 | PASS: `71aaede2` -> C, same 3/3 + 5/5; C only adds `portable_sop/` | Graph differential, direct owner oracle and portable HTTP lifecycle slices | Full lifecycle/nesting/resume matrix |
| `knowledge` | B0 Knowledge service/local provider/API | PASS: B0 -> frozen `71aaede2`, citations 3/3 and owner lifecycle 11/11 | PASS: `71aaede2` -> C, same 3/3 + 11/11; C facade is outside expected construction | Citation and 11-case lifecycle owner differentials; real-process lifecycle | Full public management/session matrix |

## 执行记录

| Case | 命令/范围 | 退出码 | 结果 |
| --- | --- | ---: | --- |
| B0-STAFF-OWNER | `PYTHONPATH="$BASE/backend:$BASE/backend/src:$BASE/portable_sop/src" /tmp/staffdeck-portable-test.S8GP21/bin/python -m pytest -q "$BASE/backend/tests/test_sop_nesting.py" "$BASE/backend/tests/test_nested_sop_execution.py" "$BASE/backend/tests/test_knowledge_base.py" "$BASE/backend/tests/test_knowledge_citations.py" "$BASE/backend/tests/test_local_knowledge_provider.py"` | 0 | 88 passed |
| N0-STAFF-NESTED-CHECK | 同一 Python 3.12 环境下，以候选 `StaffDeck-portable-sop` 作为 `PYTHONPATH` 运行冻结的 `test_sop_nesting.py` 与 `test_nested_sop_execution.py`；`ToolExecutor.execute` 通过 `SopToolAuthorization` 协议承载节点 grant，Harness 传递 detached user context | 0 | B0 冻结套件与候选均为 24/24；候选自身 `test_sop_nesting.py` + `test_tool_executor.py` 为 28/28。父节点/兄弟节点泄漏、无信任参数授权、跨 tenant/parent context 复用均拒绝。 |
| N0-STAFF-KNOWLEDGE-CROSS-PROCESS | `cd /Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop/backend && env HARNESS_V3_ENABLED=false HARNESS_ADMIN_API_ENABLED=false DEMO_SEED_ENABLED=false PUBLIC_API_ENABLED=false DATABASE_URL=sqlite:////tmp/staffdeck-knowledge-smoke.db PYTHONPATH=.:src:../portable_sop/src .venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 18194`；隔离 Python facade/API：`cd /Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop && PYTHONPATH=backend:backend/src:portable_sop/src /tmp/staffdeck-portable-test.S8GP21/bin/python -m pytest -q backend/tests/test_module_knowledge.py portable_sop/tests/test_api.py` | BLOCKED for real app startup: `app.main` still assembles Harness v3 and exits because `apps/cli/lib/bin.js` is absent, even with legacy switches disabled. The isolated facade/API owner tests pass 10/10; they are not cross-process deployment evidence. |
| N0-STAFF-KNOWLEDGE-LIFECYCLE | `python3 products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_knowledge_lifecycle.py --port 18296 --artifact-dir /tmp/staffdeck-knowledge-lifecycle-evidence` | 0 | PASS: the new Knowledge-only ASGI entrypoint starts the native StaffDeck database/ingestion owner without unrelated Harness services; real HTTP module calls cover manifest, list/create base, import, job polling, documents, query/evidence, citation resolution, then a fresh process verifies durable base/query/original citation recovery. Full correlated trace was written to the requested artifact directory. This removes the previous deployment-startup blocker for the Knowledge-only process; it does not close B0 parity or seven-slot E2E. |
| B0-PILOT-OWNER | `npm run build` in PilotDeck B0, then Node test for context compaction, Skill, read_skill and model config/usage suites | 0 | 48 passed |
| G0-N0-AGENTLOOP-PROBES | The identical `run-agent-loop-b0-differential.mjs` was invoked with `PILOTDECK_B0_ROOT` / `PILOTDECK_CANDIDATE_ROOT` for B0 -> `e55b0…`, B0 -> `fb8e983…`, B0 -> `d101edf…`, then each candidate -> current C. `e55b0…` was built at `/Users/a1/Desktop/claw/openbmb/PilotDeck-core_agent_loop_0831`; the other two were built in isolated `/tmp/pilotdeck-n0-*` worktrees. | 1 | `e55b0…` passes only pre-abort and differs in six cases. `fb8e983…` and `d101edf…` each pass single completion, tool roundtrip, and steer, but differ from both B0 and C in max-turn-after-tool, cancellation-during-work, pre-aborted-turn, and model-error-projection. Current B0 -> C passes all 7/7. The pre-existing N0 divergence remains a core baseline conflict; no N0 candidate is accepted and no comparator field was removed. |
| G0-N0-AGENTLOOP-FROZEN | Isolated `/tmp/pilotdeck-n0-parity-fixed.9QwK2V` started at non-C `148a7bd6c`. Frozen `4355463dca4e6767b0267ff4f9fee1b0b7337cea` contains two B0-compatible boundary fixes: removal of candidate-only `kind`/`text` from public `agent_status`, and an abort fence that does not persist late Tool scheduler results. It is retained on `codex/n0-agentloop-parity-20260920`. | 0 | B0 -> N0 and N0 -> C both PASS 7/7 (completion, tool roundtrip, max-turn, steer, cancellation, pre-abort and model error). `PILOTDECK_DIFFERENTIAL_TEST_INJECT_MISMATCH=1` reports FAIL and exits 1. This removes the AgentLoop N0 conflict only; it does not satisfy all-slot G0. |
| G0-N0-CONTEXT-FROZEN | The same independent N0 `4355463d` was compared. Each command accepts explicit B0 and candidate roots: `run-context-b0-differential.mjs`, `run-compaction-b0-differential.mjs`, `run-auto-compaction-b0-differential.mjs`, and `run-compaction-restart-b0-differential.mjs`. | 0 | B0 -> N0 and N0 -> C both PASS: Context 4/4 canonical/contribution/media/cache cases; manual Compaction 3/3; automatic thresholds 5/5; fresh-process restart/replay 3/3. The runner root override is harness-only and preserves fields and normalization. This closes the frozen-N0 drift for the declared Context/Compaction slices, not the full CTX-01/CMP-02 matrix. |
| G0-N0-MODEL-FROZEN | The same independent N0 `4355463d` was compared through `run-model-provider-b0-differential.mjs`, `run-model-request-b0-differential.mjs`, and `run-model-runtime-b0-differential.mjs`, each accepting explicit B0 and candidate roots. | 0 | B0 -> N0 and N0 -> C both PASS: registry 4/4, provider requests 4/4, configured runtime/stream/retry/auth/cancel/complete 6/6. `PILOTDECK_MODEL_B0_INJECT_MISMATCH=event_order` reports a Model runtime differential and exits 1. This closes the frozen-N0 drift for the declared Model slices, not the full MODEL-01 real-provider matrix. |
| G0-N0-SKILL-FROZEN | The same independent N0 `4355463d` was compared through `run-skill-b0-differential.mjs` with explicit B0 and candidate roots. | 0 | B0 -> N0 and N0 -> C both PASS 15/15: precedence/order, visibility, pagination, reads, validation/scan, create/write/delete refresh, manifest validation, import and invalid-path/source errors. Fixture-only path/time normalization and the runner's field-mutation sensitivity assertion remain unchanged. This closes the frozen-N0 drift for the declared Skill slices, not the complete SKILL-01/SKILL-02 session matrix. |
| G0-N0-TOOL-FROZEN | The same independent N0 `4355463d` was compared through registry, runtime, scheduler, side-effect and Gateway restart/cancel runners, each with explicit B0 and candidate roots. | 0 | B0 -> N0 and N0 -> C both PASS 23/23. The N0 abort fence suppresses a late `tool_execution_failed` after the aborted turn, matching B0's empty abort audit; the gateway runner's injected audit mismatch exits 1. This closes the frozen-N0 drift for the declared Tool slices, not the complete TOOL-01 matrix. |
| G0-N0-STAFFDECK-FROZEN | Isolated `/tmp/staffdeck-n0-owner.20260920` at non-C `71aaede2` was compared through `run_staffdeck_sop_graph_b0_differential.py`, `run_staffdeck_sop_finalizer_b0_differential.py`, `run_staffdeck_knowledge_b0_differential.py`, and `run_staffdeck_knowledge_owner_lifecycle_differential.py`, first with B0 as `STAFFDECK_B0_ROOT` and then with N0 as `STAFFDECK_B0_ROOT`; current C was `STAFFDECK_CANDIDATE_ROOT` for the latter. Both runs used `/tmp/staffdeck-portable-test.S8GP21/bin/python`. | 0 | B0 -> N0 and N0 -> C both PASS: SOP GraphRules 3/3, SOP TurnFinalizer 5/5, Knowledge citations 3/3, and Knowledge public owner lifecycle 11/11. `STAFFDECK_SOP_FINALIZER_INJECT_MISMATCH=1` and `STAFFDECK_KNOWLEDGE_OWNER_INJECT_MISMATCH=surface` each produced an assertion and exit 1. This closes the StaffDeck N0 path for declared owner slices, not the full SOP/Knowledge matrices or seven-slot E2E. |
| B0-C-SKILL-DIFF | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-skill-b0-differential.mjs` | 0 | 15/15 PASS: discovery precedence/order, general-chat visibility, pagination, read, validation/scan, CRUD, write-refresh-read, manifest validation, copy import and invalid-path/source errors. Fixture timestamp and isolation-path normalization retain all compared result fields. |
| B0-C-MODEL-DIFF | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-model-provider-b0-differential.mjs` plus `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-model-request-b0-differential.mjs`; B0/C `node --test --test-force-exit dist/tests/model/streaming/streamModelRetry.spec.js` | 0 | 8/8 protocol/request differential cases and 15/15 stream owner tests on both B0 and candidate: descriptors/errors, four provider request bodies, interrupted-stream continuation, idle timeout, usage/error paths and Google watchdog. Real-model behavior and broader provider usage/retry remain unverified. |
| B0-C-TOOL-DIFF | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-b0-differential.mjs` plus `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-runtime-b0-differential.mjs` and `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-scheduler-b0-differential.mjs` | 0 | 10/10 partial Tool parity: registry alias/schema/conflict/unavailable/clone/replace/unregister, runtime allow/deny/missing/setup-required/validation/execution error, and scheduler mixed-concurrency/result-slot/abort propagation. B0 lacks candidate-only scoped/hidden/dispose APIs; workspace/process side effects and audit parity remain unverified. |
| B0-C-TOOL-SIDE-EFFECTS | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-side-effects-b0-differential.mjs` | 0 | PASS for fixed workspace/process fixture: native `read_file` -> `write_file` snapshot enforcement, new-file creation, read-after-write, missing-file error, stale-write protection, shell process output redirected to a file, and core audit status/error fields match. Candidate additionally records `operationId == toolCallId` for six recorded calls; B0's audit schema predates that additive field, which is reported separately rather than erased. |
| B0-C-COMPACTION-DIFF | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-compaction-b0-differential.mjs` plus `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-auto-compaction-b0-differential.mjs` | 0 | PASS for the declared manual/automatic threshold, protected-turn, failure-cooldown, contribution-order, spill, missing-summary, cancellation, and write-race cases; restart/replay is separately recorded below and in `PARALLEL_CONTEXT_REPORT.md`. |
| B0-C-COMPACTION-RESTART | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-compaction-restart-b0-differential.mjs` | 0 | 3/3 PASS in fresh worker processes: completed manual boundary plus durable summary/tail replays after reopening JSONL, a crash tail does not expose an uncommitted replacement, and summary failure preserves prior history. Candidate-only atomic `replacementMessages` remains separately covered by its own recovery tests and is not claimed as B0 parity. |
| B0-C-CONTEXT-DIFF | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-context-b0-differential.mjs` | 0 | 4/4 B0-supported cases PASS: canonical extension/memory/tool ordering, custom-system/append/instruction contribution ordering, tool-call/result pairing with media, max-message projection and prompt-cache fields, with comparator sensitivity. Candidate-only `user_message` projection and durable context replay also PASS (one synthetic runtime-context message; instruction baseline restored without a duplicate event). B0 predates these durable/context-surface APIs, so they are not claimed as B0 parity. |
| B0-C-KNOWLEDGE-CITATION-DIFF | `/tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_knowledge_b0_differential.py`; B0 citation pytest executed against both roots | 0 | 3/3 isolated citation order/source/recovery cases and 20/20 frozen B0 citation tests on each root. Knowledge management/query lifecycle, tenant isolation and restart are unverified. |
| B0-C-KNOWLEDGE-OWNER-LIFECYCLE-DIFF | `/tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_knowledge_owner_lifecycle_differential.py` | 0 | 10/10 PASS in isolated B0 and candidate interpreters: create/import/job completion/document projection/query evidence order/citation, fresh-process recovery, generated OKF concept list/read, explicit concept upsert, lint and ZIP export entry ordering, wrong-tenant invisibility/404, optimistic update conflict, archive projection, queued-job cancellation/content cleanup, agent branch/version private head plus publish/rollback, and concurrent worker version race (one commit plus one uniqueness error). Only generated IDs/timestamps in owner-produced OKF metadata are normalized; concept path/type/content, lint and exported entry order are compared. Comparator sensitivity changed citation content and was detected. |
| PLUG-02-AGENTLOOP-UNKNOWN | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build && PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/composition/unknown-agent-loop-conformance.spec.js` | 0 | 4/4 PASS: a previously unregistered `unknown.agent-loop` is selected only by YAML, completes a real TCP Module Protocol execute, rejects wrong handshake identity and unary-only capability before execute, and preserves a structured module execution failure as an error. No factory or implementation allow-list branch was added. |
| RPC-01-HTTP-TIMEOUT-CANCEL-DISCONNECT | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build && PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/composition/http-module-runtime.spec.js`; `node --test --test-force-exit dist/tests/composition/external-sidecar-sop-session-e2e.spec.js dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js` | 0 | 18/18 HTTP conformance cases and 2/2 composition regressions PASS. A timed-out unknown Tool is dispatched once and projects `tool_timeout` with `MODULE_TIMEOUT`, `outcome: result_unknown`, and unsafe retryability; it is not replayed. A caller abort cancels in-flight HTTP dispatch and throws cancellation without a completed tool result. A server-side socket close after receiving a potentially side-effecting Tool request produces exactly one dispatch and `tool_execution_failed` with `MODULE_TRANSPORT_UNAVAILABLE/result_unknown/unsafe`; Model `prepare`, Model `stream` before its first event, Context `prepare_for_model`, Skill management `write`, and Knowledge `import_document` disconnects also remain `MODULE_TRANSPORT_UNAVAILABLE/result_unknown` after one dispatch. Delayed-response coverage directly proves Model `prepare` timeout returns `MODULE_TIMEOUT/result_unknown`, Model `stream` cancellation returns `MODULE_ABORTED/result_unknown` before an event, and Context `prepare_for_model` caller cancellation returns `MODULE_ABORTED/result_unknown`, each after one dispatch. This is only the HTTP timeout/cancel/disconnect slice; full state-mode and durable reconciliation remain unverified. |
| C-KNOWLEDGE-MANAGEMENT-E2E | `npm run build`; `node --test --test-force-exit dist/tests/composition/external-sidecar-sop-session-e2e.spec.js dist/tests/composition/http-module-runtime.spec.js`; exported `example-seven-external.yaml` checked with `docker compose config` | 0 | 8/8 composition tests. The YAML-composed sidecar/SOP/restart scenario now exercises all 32 declared Knowledge operations (base/document/job/branch, OKF, bucket/chunk, discovery, query and citation) through the same external binding; exported config retains all methods. This remains protocol/unknown-owner evidence, not StaffDeck B0 lifecycle parity. |
| C-REAL-STAFFDECK-SEVEN-SLOT | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH STAFFDECK_ROOT=/Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop STAFFDECK_PYTHON=/Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop/backend/.venv/bin/python node --test --test-force-exit dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js` | 0 | PASS, 1/1 real-process composition case on the current candidate. YAML selects all seven slots; Knowledge-only StaffDeck creates/imports/polls a real job, serves query evidence and citation, and recovers the same query/citation after its process restart. During a deliberate Knowledge outage, query fails while the SOP remains in `handoff`, then recovery succeeds. A cross-session stale resume is rejected. The Gateway reaches real `knowledge_query` through a forwarding binding with tenant/actor/base scope, external Skill/Tool/Context/Model, portable SOP handoff, TCP AgentLoop restart, duplicate-resume fencing, completion and manual compaction. The same owner case drops one import response, recovers by job/status without a duplicate request, updates and archives the document, rejects the old citation, and verifies a new citation. This is deterministic E2E-01, E2E-02 update/delete, E2E-03 outage, E2E-04 cross-session fencing, E2E-05 lost-response, and REC evidence, not the complete matrices. |
| E2E-01-FRESH-B0-REPLAY | `PILOTDECK_E2E_ARTIFACT_DIR=/tmp/pilotdeck-e2e01-current.XwQwfO` with the native-owner test, then `run-e2e01-native-pilotdeck-b0-replay.mjs`, `run_e2e01_native_staffdeck_b0_replay.py`, and `PILOTDECK_E2E_POST_COMPACTION_REPLAY=1 run-e2e01-native-pilotdeck-b0-gateway-session-replay.mjs` against the archived trace | 0 | Current C passed the real owner E2E and all three B0 replays. Trace: `products/pilotdeck-staffdeck-sop/conformance/artifacts/e2e01-native-owner-20260920-1/e2e01-native-owner-trace.json`. The B0 drivers respectively replayed native Skill/Tool, StaffDeck Knowledge/GraphRules, and Gateway/automatic-compaction restart. `PILOTDECK_E2E_REPLAY_INJECT_MISMATCH=1`, `STAFFDECK_E2E_REPLAY_INJECT_MISMATCH=1`, and `PILOTDECK_E2E_POST_COMPACTION_REQUEST_INJECT_MISMATCH=1` each exited 1. |
| REC-02-NO-REPLAY | Current C `npm run build`, then the native-owner E2E writes `PILOTDECK_E2E_ARTIFACT_DIR=/tmp/pilotdeck-e2e01-rec02.sEZO9V`; `run_e2e01_native_staffdeck_b0_replay.py` replays the archived trace; `http-module-runtime.spec.js` and `run-tool-gateway-restart-b0-differential.mjs` cover remote and native write paths. | 0 | The new trace at `products/pilotdeck-staffdeck-sop/conformance/artifacts/e2e01-native-owner-20260920-2/e2e01-native-owner-trace.json` records `lostResponseRecovery.dispatchCount: 1` and one persisted document. The B0 replay requires it; `STAFFDECK_E2E_REPLAY_INJECT_DISPATCH_MISMATCH=1` changes it to 2 and exits 1. HTTP remote Tool/Skill/Knowledge writes return fail-closed `result_unknown` after one dispatch; native Tool restart/cancel parity preserves one filesystem/process side effect. No remote durable deduplication is claimed. |
| DEP-LEGACY-SOP-RUNTIME | `PILOTDECK_API_KEY=test-only docker compose -f products/pilotdeck-staffdeck-sop/docker-compose.yml build sop-runtime`; `docker compose ... up -d sop-runtime`; in-container `/healthz`; `docker compose ... down --remove-orphans` | 0 | StaffDeck SOP runtime image built from the current candidate and reported protocol `2.0`, `sop.lifecycle/v2`, `handoff`/`external_wait` capabilities. The paired PilotDeck image build remains environment-limited because Docker Hub timed out resolving `node:22-bookworm`. |
| G5-REAL-MODEL-SMOKE | `REAL_MODEL_SOURCE_PILOT_HOME=/Users/a1/.pilotdeck STAFFDECK_SOP_SMOKE_ENDPOINT=http://127.0.0.1:18193 REAL_MODEL_SMOKE_MODEL=provider1/qwen3.6-flash-distill PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/scripts/run-real-model-smoke.mjs` | 0 | 当前候选真实模型通过 `read_file -> handoff -> resume -> completed`；handoff/completion 均有成功 `read_file` 工具事件，最终 SOP 状态为 `completed`。凭据只从运行时配置读取，未写入交付物。该 smoke 不覆盖真实 Knowledge/Compaction 七槽组合。 |
| B0-C-SOP-GRAPH-DIFF | `/tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_sop_graph_b0_differential.py` | 0 | 3/3 graph ordering/default transition, terminal slots/actions and slot-value-domain/handoff checks. Full lifecycle/nesting/resume remains unverified. |
| B0-C-AGENTLOOP-DIFF | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-agent-loop-b0-differential.mjs`; `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-agent-session-replay-b0-differential.mjs`; `node --test --test-force-exit dist/tests/agent/loop/agent-status-projection.spec.js`; `node --test --test-force-exit dist/tests/agent/modules/session-operation-ledger.spec.js dist/tests/session/agent-publication-rollback.spec.js dist/tests/agent/modules/sidecar-client.spec.js` | 0 | 7/7 execution cases plus 3/3 common transcript replay cases pass: completion, deterministic tool roundtrip, max-turn termination, multi-turn steer, cancellation during work, pre-aborted/model-error sequences, completed-message replay, compact-boundary replay and incomplete-message diagnostics match after one-to-one UUID normalization. The candidate-only recovery suite adds 4/4 ledger, 7/7 session, and 36/36 sidecar regressions: after JSONL reopen through `resumeAgentSession`, a durable known terminal is recovered across a new transport request/binding before `execute`, while a durable result_unknown is either sent to the host reconciler and resolved into the same operation journal or fails closed. A durable accepted-but-incomplete stream also fails closed; none of these recovery branches emits `execute`. Native adapter projects B0 status fields while direct sidecar composition retains its extended status fields. This is not B0 evidence for that new recovery path; safe incomplete-stream resume and the full session restore/re-execution matrix remain unverified. |
| N0-PILOT-OWNER | 同一 focused Node suite in current candidate | 0 | 50 passed（含当前新增测试） |
| C-SOP-CORE | `npm run test:sop:core` | 0 | 28 passed |
| C-SOP-EXTERNAL | `PYTHON_BIN=/tmp/staffdeck-portable-test.S8GP21/bin/python node products/pilotdeck-staffdeck-sop/conformance/run-sop-conformance.mjs` | 0 | 未登记实现的 `prepare`、`submit`、拒绝路径通过 |
| C-EXPORT-LEGACY | exporter + `PILOTDECK_API_KEY=test-only docker compose config` | 0 | legacy StaffDeck sidecar 保留 |
| C-EXPORT-EXTERNAL | exporter + `PILOTDECK_API_KEY=test-only docker compose config` | 0 | 不复制 StaffDeck、不生成 `sop-runtime` |
| C-EXPORT-MANAGED-GENERIC | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build`; `node --test --test-force-exit dist/tests/composition/export-composition.spec.js`; generated Compose `config --quiet` | 0 | 3/3 exporter cases pass: seven unknown implementation IDs produce generic `module-*` services; image/build/external modes, endpoint rewriting, environment-only credentials, external SOP dependency listing, disabled SOP omission, and health-gated dependencies are asserted. This does not prove independent container startup or owner parity. |
| DEP-01-FINAL-SEVEN-BUILD | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/scripts/export-composition.mjs --profile products/pilotdeck-staffdeck-sop/profiles/example-seven-build.yaml --out /tmp/pilotdeck-seven-final-export.djnXLE --staffdeck-root /Users/a1/Desktop/claw/openbmb/StaffDeck`; `PILOTDECK_API_KEY=test-only docker compose -p pilotdecksevenfinal -f /tmp/pilotdeck-seven-final-export.djnXLE/compose.yaml build module-agent-loop module-skills module-tools module-context module-model-provider module-knowledge module-sop`; `PILOTDECK_API_KEY=test-only docker compose -p pilotdecksevenfinal -f /tmp/pilotdeck-seven-final-export.djnXLE/compose.yaml up -d --no-build`; same Compose `restart`; manifest/TCP probes; `PILOTDECK_API_KEY=test-only docker compose -p pilotdecksevenfinal -f /tmp/pilotdeck-seven-final-export.djnXLE/compose.yaml down -v` | 0 | PASS for final exported topology and restart slice: all seven services started from the standalone artifact, six HTTP services became healthy, PilotDeck `/health` returned 200, and exported Skill/Knowledge manifests plus AgentLoop TCP execute passed before and after full Compose restart. PilotDeck used the pre-existing local `pilotdeck:exported` image; real-model seven-slot workflow and full DEP/REC matrix remain open. |

上述命令使用 Node `v22.13.1`、Python `3.12` 临时环境。测试输出未包含真实模型凭据。

## 当前差异与未完成项

1. StaffDeck B0 的嵌套 SOP grant/执行 owner 对照已通过冻结 24/24；完整 SOP 生命周期、跨进程 resume 和更广管理矩阵仍需独立覆盖。
2. SOP 以及六个 core slot 现在都有首批通用外部协议调用和未知实现测试；完整独立 conformance 矩阵、敏感性用例和 B0 → N0 → C 对照仍未完成。
3. 当前 exporter 已支持通用模块与协议 SOP 的 `deployment.mode: build|image|external`，并保留 legacy StaffDeck sidecar 路径；最终七槽 build 导出已完成独立启动、manifest/TCP 探针和全 Compose 重启切片，但真实 owner parity、真实模型和完整 DEP/REC 矩阵仍未完成。
4. 当前已有确定性的真实七槽 E2E-01、E2E-02 更新/删除路径、E2E-03 单次服务中断、E2E-04 跨会话陈旧 resume、E2E-05 单次丢响应恢复，覆盖 StaffDeck Knowledge 跨进程恢复、SOP handoff、AgentLoop sidecar 重启、resume 去重和压缩；E2E-02 的取消/竞态、更广恢复矩阵、最终七槽导出恢复和真实模型七槽 E2E 尚未由本报告覆盖。

结论：本文的 M0 owner 盘点和 M1a SOP 纵向切片是历史证据。当前 G0-G5
签收结论、两个具体剩余比较和 G4/G5 durable artifact 路径见
[ACCEPTANCE_REQUIREMENTS.md](ACCEPTANCE_REQUIREMENTS.md)。
