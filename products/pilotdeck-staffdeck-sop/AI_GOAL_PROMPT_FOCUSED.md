# 七模块目标修订草案

本稿用于替换原目标的执行方式，保留七模块范围、固定基线和 G0-G5 门槛。写入本文件不代表已修改正在运行的目标，也不代表验收通过。

```text
从现有候选继续完成 PilotDeck + StaffDeck 七模块后端装配与验收。直接实现并验证；保留用户改动，不重搭已有系统，不涉及前端。

工作目录固定为：
- /Users/a1/Desktop/claw/openbmb/PilotDeck-staffdeck-sop
- /Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop

一、交付范围与不可变约束

七个插槽：AgentLoop、Skill、Tool、Context（含自动/手动 Compaction）、Model Provider、StaffDeck SOP、StaffDeck 知识管理。
验收范围以 products/pilotdeck-staffdeck-sop/ACCEPTANCE_REQUIREMENTS.md 的行为矩阵、场景与 G0-G5 为准；本目标调整执行顺序和验证组织，不减少任何必需行为。
设计、DEVELOPMENT_SOP.md 和 ROADMAP.md 用于理解已有边界；历史进度不是当前事实。执行顺序冲突以本目标为准，语义和范围不能借此放宽。

必须保持：
- 未知实现提供模块端 adapter、manifest、配置和部署来源即可接入已支持插槽；不增加宿主厂商分支，不因接入新实现修改、构建或发布宿主。可以重启 runtime。
- PilotDeck 五个原生 owner 可用内置 Port + 静态 descriptor，免原生实现的独立 RPC 部署。StaffDeck SOP/知识真实跨进程。七槽未知实现 conformance 另行执行，不能替代原 owner parity。
- 只做装配、协议、边界映射、必要入口导出与打包；不复制或重写业务算法，不改变默认值、提示、权限、并发、排序、重试、错误、状态或事务规则。
- Skill 保留发现/scope/读取/管理/上下文消费；Context 保留预算、自动/手动压缩、spill/恢复；知识保留库/版本、文档 CRUD、job、查询和持久引用，以及 B0 已有公开管理能力。保留 B0 已有嵌套 SOP，知识内部 LLM 继续走原生依赖。
- 保留旧配置兼容。Skill/SOP/知识可关闭；仅在实际 required 依赖不满足时拒绝组合。不能用一律拒绝配置代替所需能力，也不能静默回退。

基线固定：
- PilotDeck B0：ecedc5c32f2b8c8e5387cb2faba70cccf65650fd
- StaffDeck B0：7adc7c84f61bd6cca13ff0380a811cbb3ae3c544
隔离运行 B0，产品源码不打补丁。区分 B0、冻结的解耦原生 N0、候选 C；分别保留 B0→N0、N0→C、B0→C 证据。既有分支漂移不自动豁免；不能把当前 C 同时当成 N0。

二、开始一次，之后从断点继续

首次接手读取适用 AGENTS.md、上述规范以及 ACCEPTANCE.md、BASELINE_AUDIT.md、REMAINING_TEST_PLAN.md。只核对相关代码、两树 HEAD/dirty 状态和证据，不先重跑所有历史测试。
将现有验收 ID 映射到已有证据与具体缺口，利用 REMAINING_TEST_PLAN.md 的现有表格维护状态，不新建一套重复路线图。保留原始失败与后续解决记录。
若本任务已有接手记录，只检查新增改动与当前待办，不重复完整盘点。上下文压缩、轮次续跑、工作区切换或用户询问状态，均从当前工作项继续；除非用户明确取消或替换目标。

三、一次只推进一个可验收工作项

当前工作项只记录五项：验收 ID、缺失行为、涉及调用路径、通过条件/验证命令、下一步动作。
选题顺序为：已证实的语义错误 → 阻断真实组合的功能缺口 → 缺失的验收证据。不能因为另一个测试更容易补就换题。
从实现到聚焦验证连续完成当前项。小补丁提交、测试数量增加、文档更新不等于工作项完成。
仅在以下情况切换：当前项通过；已定位到具体外部依赖或核心语义冲突而无法继续；用户改变优先级。切换时留下恢复条件与下一步，再选择无依赖的待办。
连续两次尝试同一症状没有新证据时，重新检查假设和最早分歧，不原样重复操作，也不扩大测试范围碰运气。

按以下工作包收口，已满足部分直接复用，不重做：

A. 修正已发现的语义与验收缺陷。
先核查当前代码是否仍有：跨 Tool owner 分组串行化、AgentLoop 仅兼容入口符合 B0 而实际组合入口不同、Knowledge 禁用被当作必需模块错误、差分失败仍退出 0。
逐项用实际调用路径复现、修复对应边界、运行回归；已被其他改动修复的项目确认后跳过。不能为了 parity 给测试专设一条与产品执行不同的路径。
退出条件：这些具体问题有解决证据，或有明确阻塞与影响范围。

B. 完成真实 owner 的主组合场景 E2E-01。
优先扩展现有 Gateway/E2E runner，避免另建同功能框架。实际执行知识入库/job、Skill 读取、Tool、知识查询/引用、Context 自动与手动压缩、SOP handoff、重启后 resume、completed。
PilotDeck 原生 owner 可以内置，StaffDeck SOP/知识跨进程；确定性测试只固定模型/摘要等外部响应，不用 fixture 替代正在验收的 owner。
保存组合实际模块请求、结果、状态和副作用；用独立 B0 driver 在等价状态下重放，覆盖真实装配入口。既有 fixture 七槽测试仍作为 conformance 证据，不升级为真实 owner E2E。
退出条件：E2E-01 所需行为和对应实际调用 parity 通过；发现的差异已闭环。存在阻塞则保留未通过状态，仅转向独立工作。

C. 补齐剩余验收矩阵。
按已有 ID 逐项补齐：逐模块完整行为与三层对照、七槽未知实现接入、Skill/SOP/知识开关与依赖、runtime 重建、E2E-02..05、协议错误和 REC 恢复。
每项先复用已有有效用例，只为缺失的行为/断言增加测试。同一条真实 trace 可以支持多个 ID，但必须分别满足各自断言；不能把局部覆盖写成整个 ID PASS。
恢复要求必须验证恢复结果；fail-closed 只证明没有不安全重发，不能代替要求的 resume/reconcile 能力。
验收覆盖由已有场景及 B0 公开能力盘点确定，不穷举没有要求的模块/故障笛卡尔积，也不自行追加产品功能。新发现的范围内缺失行为仍须纳入对应 ID。
退出条件：代码与确定性验收的剩余必需项闭环，留下的外部阻塞可明确列举。

D. 最终候选导出与真实模型验收。
在代码收口后冻结候选，执行 build/image/external 必需导出、脱离原工作树的部署、重启/共享依赖/故障隔离和 E2E-REAL。已有针对导出器的快速配置测试可以提前执行，不必每个小补丁重建全部镜像。
真实模型优先用本机 provider1/qwen3.6-flash-distill；凭据从 /Users/a1/.pilotdeck 运行时读取，不打印、提交或放入导出包。确认真实业务链和一次真实摘要调用，严格差分使用确定性场景。
退出条件：G0-G5 全部必需门槛通过，产物与最终候选一致。

四、验证只为解决具体不确定性

- 修改同一行为的一组文件后统一构建，运行最小相关回归；调度、状态或上下文等共享行为必须包含实际组合回归。
- 用例通过且相关源码/配置/依赖未变时沿用该证据。仅因跨轮次、修改文档或测试数量变化，不重跑构建或套件。最终冻结候选按全部必需门槛执行一次权威验收；之后有改动再重跑受影响部分。
- 原 owner parity 与第三方 conformance 分开。第三方只需满足公开契约和声明能力，不要求复制原算法。冻结宿主后新增独立实现，真正执行领域操作及错误路径；若为此修了宿主，重新冻结并重跑受影响接入验收。
- expected 不调用候选转换逻辑。比较字段预先固定，仅一一映射非语义 ID、时间和隔离路径，保留请求、因果顺序、状态、usage、引用和副作用计数。
- 比较器发现差异必须报告 FAIL 并非零退出；共同异常不算成功。注入顺序/错误/状态/引用/压缩边界差异，确认 runner 和报告都能拒绝。不能删字段、重排事件、改产品并发或降级 result_unknown 来转绿。
- harness 修复只限观察、关联与记录缺陷，保留原始证据，证明产品输入/结果没变。B0 漂移优先通过复用原 owner 或边界适配解决；若只能修改核心规则、换基线或缩范围，报告具体冲突，停止依赖该冲突的工作。

五、阻塞与进度记录

外部阻塞记录一次：命令/退出码、原始错误、受影响 ID、恢复条件、下一个独立工作项。只有依赖状态变化或有新的具体修复假设时重试；Docker Hub 无变化时不反复 pull/build。
一个阻塞只阻断依赖它的工作，不把整个项目停住。所有可执行独立工作完成后，如实报告剩余外部阻塞，不用重复测试维持“持续工作”，也不声明目标完成。
进行中只更新 REMAINING_TEST_PLAN.md 的当前项与断点；原始日志随执行保留。每完成一个工作项，在 ACCEPTANCE.md 记录其证据一次。BASELINE_AUDIT.md 仅在基线/漂移结论变化时更新，ROADMAP.md 仅在阶段状态变化时更新，不每轮同步全部文档。
进度说明只回答：本轮关闭了哪个行为缺口、证据是什么、下一项是什么。没有关闭时说明新定位到的原因。不要用累计测试数代替完成度，也不要每轮重列全部 G0-G5。

六、完成判定与交付

只有 G0-G5 全部必需门 PASS 才声明 READY。范围内未实现、未验证和 BLOCKED 均不能改称完成或不适用；不通过缩减矩阵降低最终门槛。
交付代码、公开契约/示例/profile、导出与可复现 runner、独立基线驱动、长期保存的脱敏 trace/比较报告。保留覆盖范围与未覆盖项，不承诺测试已证明所有可能输入等价。
最终报告：实际行为变化；G0-G5 状态与完整命令/退出码/产物位置；首次差异及其解决证据；剩余阻塞/未运行项。未全通过时结论为 NOT READY。
不主动创建 PR、推送、合并或发布。不得用推进了其他小项替代当前工作项收口。
```
