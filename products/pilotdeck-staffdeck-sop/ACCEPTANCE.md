# PilotDeck + StaffDeck SOP Acceptance

Date: 2026-09-18

## Scope

This profile composes PilotDeck AgentLoop, tools, and model providers with the
portable StaffDeck SOP runtime. Preserving existing module semantics is a
release requirement, not a conclusion established by the passing tests below.

## Acceptance Decision

The current result is **READY** for the requested seven-slot pluggable
composition. The candidate has real session-level Knowledge consumption, a
declared 32/32 Knowledge facade matrix and 11/11 owner lifecycle groups, plus
generic YAML bindings and direct unknown-implementation tests. The authoritative
G0-G5 table is fully PASS; this document is the release sign-off for the stated
scope.

The retired historical legacy-versus-sidecar runner remains unavailable and is
recorded as a harness limitation; it is not silently converted into PASS.

A focused diagnostic reproduced a semantic divergence in the former portable
engine: for a required `name` slot containing three spaces, StaffDeck's
original `slot_is_filled` returns `False`, while that copied implementation
accepted completion. The copied engine has been removed. The HTTP sidecar now
calls StaffDeck's existing `SopResultValidator`, `GraphRules`, and
`SopRuntime`; its whitespace response is `422 REQUIRED_SLOT_MISSING`.
Host-side state projection preserves the terminal frame and clears only the
per-step PilotDeck tool receipts.

See [REMAINING_TEST_PLAN.md](REMAINING_TEST_PLAN.md) for the ordered test plan,
source references, execution prerequisites, and release criteria.

## Historical And Partial Evidence

These are previously recorded results, not reruns during the acceptance review.

| Gate | Command | Result |
| --- | --- | --- |
| SOP composition and recovery | `npm run test:sop:core` | 16/16 PASS |
| Gateway to real StaffDeck HTTP runtime | `STAFFDECK_SOP_E2E_ENDPOINT=... npm run test:sop:http-e2e` | 7/7 PASS |
| Cross-process handoff/resume | `node products/pilotdeck-staffdeck-sop/scripts/run-process-restart-smoke.mjs` | PASS, duplicate resume rejected |
| StaffDeck portable runtime | `python -m pytest -q portable_sop/tests` | 10/10 PASS |
| Related config/Gateway contracts | focused Node test command | 37/37 PASS |
| Web SOP route | `npm --prefix ui test -- --run server/routes/sop.test.js` | 8/8 PASS |
| Web SOP banner | `npm --prefix ui test -- --run src/components/chat-v2/SopWaitBanner.test.tsx` | 2/2 PASS |
| TypeScript and production UI | `npm run build:web` | PASS |
| Deployment manifest | `PILOTDECK_API_KEY=test-only docker compose -f products/pilotdeck-staffdeck-sop/docker-compose.yml config` | PASS |
| Browser workflow | `read_file -> handoff -> refresh -> resume -> completion` | PASS; one completion reply, no wait banner |

## Current Candidate Evidence (Partial)

| Gate | Command | Result |
| --- | --- | --- |
| Generic core module bindings | `npm run build`; focused config and HTTP-module tests | PARTIAL PASS: unknown Model/Tool/Context/Skill/Knowledge calls use contract + manifest + YAML without implementation whitelists. The Skill adapter preserves runtime discovery/read plus SkillManager CRUD/validate/import/scan, and the Knowledge adapter forwards the declared management, job, query and citation operations. This is not B0 parity evidence. |
| PLUG-02 all-slot unknown implementation matrix | `env -u NODE_OPTIONS PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/composition/unknown-agent-loop-conformance.spec.js dist/tests/composition/external-sidecar-sop-session-e2e.spec.js dist/tests/composition/http-module-runtime.spec.js dist/tests/pilot/config/modules-config.spec.js`; `PYTHON_BIN=/tmp/staffdeck-portable-test.S8GP21/bin/python node products/pilotdeck-staffdeck-sop/conformance/run-sop-conformance.mjs` | PASS for the declared PLUG-02 matrix: 44 Node cases plus the standalone SOP conformance. YAML selects an unregistered AgentLoop through TCP and unregistered Model, Tool, Context, Skill, Knowledge, and SOP implementations through generic transports. Each performs a domain operation; identity/capability/error paths, protocol/timeout/disconnect errors, SOP rejection, and contract/state-mode validation reject without a host factory or implementation allow-list. PLUG-03/04/05 and RPC-01 are separately PASS in `PARALLEL_PROTOCOL_REPORT.md`; this row no longer leaves a toggle audit open. |
| HTTP module timeout/cancel/disconnect conformance | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build && PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/composition/http-module-runtime.spec.js`; `node --test --test-force-exit dist/tests/composition/external-sidecar-sop-session-e2e.spec.js dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js` | PASS for the declared RPC-01 matrix: 18/18 HTTP-module cases plus 2/2 composed regressions cover one-dispatch timeout, caller cancellation, disconnect/result-unknown, and contradictory Knowledge response after SOP state. Durable reconciliation remains limited to the explicit contract; no undeclared remote deduplication is claimed. |
| Generic managed module exporter | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build`; `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/composition/export-composition.spec.js`; generated artifact `PILOTDECK_API_KEY=test-only docker compose -f <artifact>/compose.yaml config --quiet` | PASS, 3/3 exporter cases: a frozen unknown-implementation profile emits `module-agent-loop`, `module-skills`, `module-tools`, `module-context`, `module-model-provider`, `module-knowledge`, and `module-sop` without slot-specific exporter branches; image, build, and external sources rewrite endpoints/host/ports, preserve environment-only credentials, list external SOP dependencies, and omit disabled SOP. Health-gated services use `service_healthy`. This is packaging/config evidence, not independent seven-module startup or parity evidence. |
| Final seven-slot build export deployment | `export-composition.mjs` with the final profile; Compose config/build/start/restart and manifest/TCP probes | PASS for the frozen native-five artifact, with durable sanitized evidence at `conformance/artifacts/g4-native-five-runtime-20260920/`: the artifact built as `pilotdeck:g4-native-five-20260920` using the cached `node:22-bookworm-slim` base tagged locally for the declared Dockerfile base. On an isolated Docker network, current StaffDeck SOP and Knowledge services returned 200; PilotDeck returned 200; in-network Knowledge calls succeeded. The wait ID survived PilotDeck restart, Knowledge state survived independent restart, and stopping SOP produced an actionable fetch failure without false completion before recovery. The generated artifact was not modified. |
| Architecture boundary | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH ./node_modules/.bin/tsc -p tsconfig.json` then `node --test --test-force-exit dist/tests/composition/architecture-boundary.spec.js` | 2/2 PASS against architecture commit `e55b0a82d07ee3951e5812c34103400dfa6043f`: core AgentLoop/module transport has no reverse owner imports; host composition selects Ports and SOP remains a decorator. This does not prove behavioral parity. |
| B0 SkillManager differential | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-skill-b0-differential.mjs` | PASS, 15/15 independent B0-versus-candidate cases: discovery precedence/order, general-chat visibility, query pagination, reads, validation/scan, CRUD, write-refresh-read, manifest validation, copy import and invalid path/source errors. Isolation paths and filesystem timestamps are explicitly normalized while retaining all result fields; the comparator has a changed-field sensitivity assertion. |
| Native Skill Gateway profile boundary | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build && PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit --test-name-pattern='native PilotDeck owners compose real StaffDeck Knowledge and SOP through Gateway' dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js` | PASS, 1/1 real-owner Gateway E2E. With native Skills enabled, the model request contains `read_skill` and the project Skill body, and the deterministic model dispatches the read. The same fixture then rewrites only `skills.enabled: false`, recreates the Gateway, and reaches `read_file` + StaffDeck Knowledge query + real SOP handoff with no `read_skill` tool, Skill body, or `read_skill` dispatch. This closes the declared SKILL-01/SKILL-02 session-consumption and enabled/disabled slice; the independent B0 manager differential above remains its discovery/management parity evidence. |
| B0 Model Provider differentials | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-model-provider-b0-differential.mjs` and `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-model-request-b0-differential.mjs`; B0/C `node --test --test-force-exit dist/tests/model/streaming/streamModelRetry.spec.js` | PASS for the declared deterministic B0/C provider/request slice, 8/8 differential cases and 15/15 stream owner tests on both B0 and candidate: protocol descriptors/errors, four request mappings, idle timeout, interrupted-stream continuation, usage/error handling and Google watchdog behavior. Real-provider workflow is covered separately by G5. |
| B0 Model runtime differential | `env -u NODE_OPTIONS PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-model-runtime-b0-differential.mjs`; `env -u NODE_OPTIONS PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/model/streaming/streamModelRetry.spec.js dist/tests/model/normalizeUsage.spec.js dist/tests/model/errors/normalizeModelError.spec.js dist/tests/model/retry-policy.spec.js` | PASS for the deterministic B0 Model runtime slice: 6/6 B0/C cases cover configured provider/model selection and default token cap, full normalized text/tool/usage stream, rate-limit retry request/progress counts, auth error classification, active in-flight cancellation, and non-stream tool/usage response. The focused owner regression passes 26/26. `PILOTDECK_MODEL_B0_INJECT_MISMATCH=usage`, `error`, or `event_order` each produces an assertion failure and exit 1. This does not substitute for the final real-provider seven-slot G5 workflow. |
| B0 Tool differentials | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-b0-differential.mjs`, `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-runtime-b0-differential.mjs`, and `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-scheduler-b0-differential.mjs` | PARTIAL PASS, 10/10 B0-versus-candidate cases: registry registration/alias/schema order, conflicts/unavailable diagnostics, clone/replace/unregister, ToolRuntime allow/deny/missing/setup-required/input-validation/execution-failure, and scheduler mixed concurrency, result-slot ordering and abort propagation. B0 lacks the candidate's additional scoped/hidden/dispose APIs, which are not counted as parity; workspace file/process side effects and audit remain open. |
| B0 Tool differentials | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-b0-differential.mjs`, `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-runtime-b0-differential.mjs`, and `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-scheduler-b0-differential.mjs` | PARTIAL PASS, 10/10 B0-versus-candidate cases: registry registration/alias/schema order, conflicts/unavailable diagnostics, clone/replace/unregister, ToolRuntime allow/deny/missing/setup-required/input-validation/execution-failure, and scheduler mixed concurrency, result-slot ordering and abort propagation. B0 lacks the candidate's additional scoped/hidden/dispose APIs, which are not counted as parity; workspace file/process side effects and audit remain open. |
| B0 Tool side-effect differential | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-side-effects-b0-differential.mjs` | PARTIAL PASS: workspace/process side effects, read-after-write, missing-file error, stale-write protection and core audit fields match B0; candidate also emits additive `operationId == toolCallId` audit fields for six recorded calls, reported explicitly because B0's frozen audit schema has no such field. Broader filesystem/process matrix and durable audit restart remain open. |
| Native Gateway Tool restart/cancel differential | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-tool-gateway-restart-b0-differential.mjs`; `PILOTDECK_DIFFERENTIAL_TEST_INJECT_MISMATCH=1 ...` | PASS for the declared TOOL-01 native Gateway slice. B0 and candidate match `read_file`, `write_file`, and `bash` results, durable model-visible audit order, workspace/process side effects, no replay after Gateway restart, native timeout, and cancellation after Tool dispatch. A cancelled late scheduler result is no longer recorded as a settled durable Tool result, preserving B0's empty audit plus `aborted_streaming` terminal. The injected tool-name mismatch exits nonzero. |
| B0 Context/Compaction differentials | `run-compaction-b0-differential.mjs`, `run-auto-compaction-b0-differential.mjs`, `run-compaction-restart-b0-differential.mjs`, native recovery, spill/missing-summary and write-race tests | PASS for the declared CTX-01/CMP-01/CMP-02 cases: thresholds, failure cooldown, summary failure/cancellation, kill-before/after-boundary, spill reference recovery, manual missing-summary restart, cancellation/write-race around the atomic replacement append, and bounded E2E-01 replay. The refreshed full post-restart request is canonical on messages, tool names, `agent`, and `execute_code` descriptions. |
| B0 Context request/replay differential | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-context-b0-differential.mjs` | PASS for the declared 4/4 B0-supported cases: canonical extension/memory/tool ordering, custom/append/instruction contribution ordering, tool-call/result pairing with media, max-message projection and prompt-cache fields. Candidate `user_message` runtime-context projection and durable recorder restart/replay also pass, including no duplicate instruction baseline after restore. |
| B0 StaffDeck Knowledge owner/facade differential | `/tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_knowledge_owner_lifecycle_differential.py`; `run_staffdeck_knowledge_lifecycle.py` | PASS for the declared Knowledge matrix: 11/11 B0/C owner lifecycle groups and 32/32 public facade calls, with query/citation/OKF state across restart and mismatch sensitivity. This does not establish G0, G3, G4, or G5. |
| B0 StaffDeck SOP graph differential | `/tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_sop_graph_b0_differential.py` | PARTIAL PASS, 3/3 isolated B0-versus-candidate cases for graph/edge priority, default transition, terminal slot/action conditions, slot value domain and handoff search. This does not cover full SOP lifecycle, nested SOPs or durable resume. |
| B0 StaffDeck SOP finalizer and real Gateway lifecycle | `PYTHONPATH=/Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop/backend:/Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop/backend/src /tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_sop_finalizer_b0_differential.py`; `PYTHONPATH=backend:backend/src:portable_sop/src /tmp/staffdeck-portable-test.S8GP21/bin/python -m pytest -q portable_sop/tests/test_owner_contract.py portable_sop/tests/test_api.py portable_sop/tests/test_engine.py`; `env -u NODE_OPTIONS STAFFDECK_SOP_E2E_ENDPOINT=http://127.0.0.1:8091 PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/sop/staffdeck-sop-gateway-http-e2e.spec.js` | PASS for the declared `SOP-01/SOP-02` owner lifecycle slice: B0/C finalizer is 5/5 for allowed/routed/rejected handoff, completion, and continuation; the direct owner/API suite is 49/49; the real HTTP Gateway suite is 10/10. Handoff and external-task states create the only resumable waits. `awaiting_user` and `failed` have no wait, reject `resumeSop`, and complete through a normal subsequent Gateway turn. `blocked` has no wait, rejects `resumeSop`, and remains terminal with no SOP revision change on an ordinary turn. `STAFFDECK_SOP_FINALIZER_INJECT_MISMATCH=1` reverses the routed side effects and exits nonzero. |
| B0 AgentLoop differential | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-agent-loop-b0-differential.mjs`; `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/conformance/run-agent-session-replay-b0-differential.mjs`; `node --test --test-force-exit dist/tests/agent/loop/agent-status-projection.spec.js`; `node --test --test-force-exit dist/tests/agent/modules/session-operation-ledger.spec.js dist/tests/session/agent-publication-rollback.spec.js dist/tests/agent/modules/sidecar-client.spec.js` | PARTIAL PASS: 7/7 execution cases plus four B0/C session-replay cases after one-to-one ID normalization cover completion, tool roundtrip, max-turn, steer, cancellation, pre-abort, model error, completed-message replay, compact-boundary replay, incomplete-message diagnostics, and an actual resumed incomplete turn. The resumed case restores a durable user input while excluding its incomplete assistant output, submits a new turn through each owner's native session entry, and compares the full model-visible request order, visible event order, terminal result, common durable semantic record order, and zero tool side effects; an injected message change exits 1. Candidate-only step/model-stream ledger records are retained as additive observability, not normalized into the B0 result. A focused JSONL recovery path restores a settled sidecar operation through `resumeAgentSession`, re-handshakes a replacement sidecar, returns the durable terminal without a second `execute`, and preserves the original three-entry operation journal. It also sends a persisted result_unknown to the host reconciler before re-execution, appends the resolved terminal to that same operation, and does not send `execute`; without a reconciler, unknown and incomplete operations fail closed without `execute`. Ledger, session, and full sidecar regressions pass 4/4, 7/7, and 36/36. Native compatibility composition keeps B0 status shape; direct sidecar composition retains extended status fields. This closes the declared LOOP-01 incomplete-stream/resumed-execution slice, not the complete AgentLoop matrix. |
| External Skill/Knowledge Gateway session | `node --test --test-force-exit dist/tests/composition/external-session-e2e.spec.js` | PARTIAL PASS: one Gateway turn reaches unregistered external Model, Tool, Context, Skill and Knowledge modules; external Skill-backed `skillsList`/`skillRead` and model `read_skill`, external tool and host-owned `knowledge_query` all execute. No SOP or compaction is in this same scenario. |
| YAML-composed external AgentLoop + SOP + core modules | `npm run build`; `node --test --test-force-exit dist/tests/composition/external-sidecar-sop-session-e2e.spec.js dist/tests/composition/http-module-runtime.spec.js`; exported `example-seven-external.yaml` checked with `docker compose config` | PASS, 8/8 focused composition tests: YAML-selected TCP AgentLoop reaches external Skill/Tool/Context/Model/Knowledge, and the same binding accepts all 32 declared Knowledge management/job/OKF/discovery/query/citation operations. The scenario performs external automatic and manual compaction, continues SOP after tool results, persists `completed`, persists a second-session handoff, restarts the TCP sidecar, deduplicates repeated `resumeSop`, and completes the continuation. StaffDeck-service restart and B0 parity remain outside this case. |
| Real StaffDeck seven-slot Gateway composition | `env -u NODE_OPTIONS PILOTDECK_E2E_ARTIFACT_DIR=/tmp/pilotdeck-e2e01-rec02.sEZO9V PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit --test-name-pattern='native PilotDeck owners compose real StaffDeck Knowledge and SOP through Gateway' dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js`; then run `run-e2e01-native-pilotdeck-b0-replay.mjs`, `run_e2e01_native_staffdeck_b0_replay.py`, and `PILOTDECK_E2E_POST_COMPACTION_REPLAY=1 run-e2e01-native-pilotdeck-b0-gateway-session-replay.mjs` against `conformance/artifacts/e2e01-native-owner-20260920-2/e2e01-native-owner-trace.json` | PASS for the declared native-owner composition. The real-owner Gateway covers Skill discovery/read, `read_file`, Context, deterministic Model transport, Knowledge scope/query/citation, SOP handoff/resume/completion, automatic/manual compaction, lost import response recovery, document update/archive, outage/restart, and cross-session resume fencing. The strict B0 Gateway replay compares the complete 28-message post-restart request; Skill, citation, dispatch-count, and request mismatch injections exit `1`. The archived trace and focused recovery tests cover contribution ordering, spill/missing-summary, cancellation/write-race, and service recovery. Fixed B0 lacking seven-slot bindings or `sop.lifecycle/v2` is not itself a blocker: the requirement is independent owner replay in equivalent state. |
| E2E-02 native Knowledge document lifecycle | `PILOTDECK_E2E_ARTIFACT_DIR=<dir> node --test --test-force-exit --test-name-pattern='native PilotDeck owners' dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js`; `python3 products/pilotdeck-staffdeck-sop/conformance/run_e2e01_native_staffdeck_b0_replay.py <schema-v3-trace>` | PASS: the real native-owner Gateway scenario updates the imported document, rejects the old citation, resolves updated content, archives the document, and observes zero returned evidence. The frozen B0 owner driver compares updated citation content, stale-citation rejection, archive status, and post-archive evidence count; mismatch injection exits `1`. |
| E2E-03 native Knowledge outage/recovery | `PILOTDECK_E2E_ARTIFACT_DIR=<dir> node --test --test-force-exit --test-name-pattern='native PilotDeck owners' dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js`; `python3 products/pilotdeck-staffdeck-sop/conformance/run_e2e01_native_staffdeck_b0_replay.py <schema-v3-trace>` | PASS: stopping Knowledge during SOP handoff leaves SOP revision/state/wait unchanged; restart against the same SQLite database restores the citation and completion path. The B0 comparator rejects changed pre/post-outage state or citation. |
| E2E-04 native cross-session resume isolation | `PILOTDECK_E2E_ARTIFACT_DIR=<dir> node --test --test-force-exit --test-name-pattern='native PilotDeck owners' dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js`; `python3 products/pilotdeck-staffdeck-sop/conformance/run_e2e01_native_staffdeck_b0_replay.py <schema-v3-trace>` | PASS: a second session cannot resume the first wait; the first session's revision/state/wait remains unchanged and only its valid resume completes. |
| E2E-05 native lost import response recovery | `PILOTDECK_E2E_ARTIFACT_DIR=<dir> node --test --test-force-exit --test-name-pattern='native PilotDeck owners' dist/tests/composition/real-staffdeck-seven-slot-e2e.spec.js`; `python3 products/pilotdeck-staffdeck-sop/conformance/run_e2e01_native_staffdeck_b0_replay.py <schema-v3-trace>` | PASS: one import is dispatched, only its response is lost, job/status finds exactly one document, and no import retry occurs. The B0 comparator rejects a missing lost-response proof or document count other than one. |
| AgentLoop configured identity and SOP decoration | focused `sidecar-client.spec.js`; `staffdeck-sop-agent-loop.spec.js`; `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build && PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/composition/unknown-agent-loop-conformance.spec.js` | PARTIAL PASS: 4/4 unknown-implementation conformance cases pass. Protocol version and configured `implementationId` mismatch are rejected before execute; a previously unregistered TCP implementation completes a real execute through YAML-only binding; unary-only capability is rejected before execute; structured module failure remains an error. SOP glue decorates an external runner with host-owned context and `submit_step_result` control Ports. Combined sidecar restart/wait recovery now has evidence; independent owner parity remains unverified. |
| Context/Compaction regression | focused auto/manual compaction, lifecycle, replacement and replay tests | PASS, 65/65 in the focused candidate suite plus 3/3 fresh-worker B0/C restart cases: automatic/manual trigger, failure, durable replacement and replay behavior remain covered. Contribution ordering, spill/missing-summary, cancellation/write-race, and native-five durable compaction are covered by the companion reports and artifacts. |
| StaffDeck Knowledge owner and facade | `PYTHONPATH=backend:backend/src:portable_sop/src /tmp/staffdeck-portable-test.S8GP21/bin/python -m pytest -q backend/tests/test_module_knowledge.py` | PASS, 5/5 facade tests: the 32-operation manifest, error correlation, persisted citation projection, and native OKF concept/list/lint/ZIP-export forwarding pass. The real-process matrix below supplies the public management coverage. |
| StaffDeck Knowledge real lifecycle/restart | `STAFFDECK_PYTHON=/tmp/staffdeck-portable-test.S8GP21/bin/python /tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_knowledge_lifecycle.py --artifact-dir /tmp/staffdeck-knowledge-artifacts` | PASS for the real StaffDeck Knowledge-only process: it calls all 32 declared facade operations, including base/document/bucket/chunk management, job list/cancel, OKF import, discovery confirmation, deletion, query/citation, and all branch-only owner errors in the overall-only deployment. It restarts against the same SQLite database and preserves query, original citation and OKF concepts. Each envelope is retained in the artifact trace; `list_document_buckets` now imports and delegates to the native owner. |
| StaffDeck B0 -> N0 -> C owner inventory | With `STAFFDECK_PYTHON=/tmp/staffdeck-portable-test.S8GP21/bin/python`, run `run_staffdeck_sop_graph_b0_differential.py`, `run_staffdeck_sop_finalizer_b0_differential.py`, `run_staffdeck_knowledge_b0_differential.py`, and `run_staffdeck_knowledge_owner_lifecycle_differential.py` twice: `STAFFDECK_B0_ROOT=/tmp/pilotdeck-staffdeck-m0.j4voeS/staffdeck-b0 STAFFDECK_CANDIDATE_ROOT=/tmp/staffdeck-n0-owner.20260920`, then `STAFFDECK_B0_ROOT=/tmp/staffdeck-n0-owner.20260920 STAFFDECK_CANDIDATE_ROOT=/Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop`. | PASS for the declared native owner slices: SOP GraphRules 3/3, TurnFinalizer 5/5, Knowledge citations 3/3, and Knowledge public lifecycle 11/11 on both B0 -> N0 and N0 -> C. N0 is non-C `71aaede2`; C adds `portable_sop/` without altering these native owners. Injecting `STAFFDECK_SOP_FINALIZER_INJECT_MISMATCH=1` or `STAFFDECK_KNOWLEDGE_OWNER_INJECT_MISMATCH=surface` exits 1. This is G0/EVID-01 inventory evidence only, not full G0-G5 sign-off. |
| StaffDeck Knowledge B0 -> candidate owner differential | `STAFFDECK_PYTHON=/tmp/staffdeck-portable-test.S8GP21/bin/python /tmp/staffdeck-portable-test.S8GP21/bin/python products/pilotdeck-staffdeck-sop/conformance/run_staffdeck_knowledge_owner_lifecycle_differential.py` | PASS, 11/11 independent behavior groups: list/create base, import and persisted job, lexical evidence order/content, citation resolution, fresh-process recovery, generated OKF concepts plus explicit upsert/lint/ZIP export, base/document/bucket/chunk/job/discovery public-owner surface, tenant isolation/404 plus optimistic update conflict, document archive projection plus queued-job cancellation cleanup, agent branch/version private head plus publish/rollback, and concurrent worker version race. B0 and candidate run in separate interpreters/databases; only generated IDs/timestamps embedded in OKF source metadata are normalized, while concept paths/types/content and native owner outcomes remain compared. |
| StaffDeck adapter core | Containerized direct `prepare`/`submit` against original StaffDeck module | PASS; valid tool/slot advance and whitespace rejection |
| v3 SOP module contract | PilotDeck build and focused tests; portable `pytest`; real HTTP Gateway suite | PASS: `sop.runtime` advertises `sop.lifecycle/v2` protocol `2.0`; envelopes, semantic error pass-through, identity, idempotency, revision fencing, and owner-vs-glue parity passed (`21/21`, `28/28`, `9/9`). |
| Gateway to current adapter | `STAFFDECK_SOP_E2E_ENDPOINT=http://127.0.0.1:18091 node --test --test-force-exit dist/tests/sop/staffdeck-sop-gateway-http-e2e.spec.js` | 9/9 PASS, including enabled -> disabled -> enabled restoration and a declared-but-unbound `lookup_account` fixture path. |
| SOP recovery suite | `node --test --test-force-exit dist/tests/sop/staffdeck-sop-agent-loop.spec.js` | 15/15 PASS, including a child-process `SIGKILL` after reply durability and before delivery-journal cleanup. Recovery re-homes exactly one visible terminal reply without a second model invocation or SOP submission. |
| Cross-process handoff/resume | `node products/pilotdeck-staffdeck-sop/scripts/run-process-restart-smoke.mjs` | PASS; `sopStatus: completed`, duplicate resume rejected |
| YAML export profiles | `export-composition.mjs` plus `docker compose config` | PASS for SOP enabled and disabled profiles; build contexts are inside each export |
| Seven-slot external export topology | `PILOTDECK_API_KEY=test-only docker compose -f /tmp/pilotdeck-seven-export.pu7htR/compose.yaml config` | PASS for the generated `example-seven-external.yaml` artifact: all six external core bindings plus external SOP are retained in the copied config, with credentials supplied only at Compose invocation. This is packaging/config evidence, not deployment or parity evidence. |
| Local browser workflow, current adapter | `run-local-web-smoke.sh` with the Node 22.23.1 runtime; manual browser exercise | PASS at desktop and 390x844: `read_file` permission/tool result -> SOP handoff -> page refresh -> recoverable continuation -> one terminal reply. Mock provider only. |
| Exported SOP-enabled deployment boot | `PILOTDECK_API_KEY=test-only docker compose --project-name pilotdeckexportverify ... up --detach` | PASS: both images built from `/tmp/pilotdeck-export-deploy.qpGccG`; `sop-runtime` became healthy, PilotDeck HTTP returned 200, and its Gateway connected. No model turn was sent. |
| Current legacy SOP runtime rebuild | `PILOTDECK_API_KEY=test-only docker compose -f products/pilotdeck-staffdeck-sop/docker-compose.yml build sop-runtime`; Compose start; in-container `/healthz`; Compose teardown | PASS for the current StaffDeck candidate runtime image and protocol health (`2.0`, `sop.lifecycle/v2`, handoff/external-wait). The paired PilotDeck image rebuild was environment-limited by a Docker Hub timeout resolving `node:22-bookworm`. |
| Portable adapter state recovery | isolated Python 3.12 venv, `pytest -q portable_sop/tests` | 22/22 PASS after restoring the native coordinator's `awaiting_user -> active` transition before a completed resumed submission; slot matrix and handoff/wait/transition/capability-receipt contracts preserve original semantics. |
| Original SOP composition contract | isolated Python 3.12 venv, `pytest backend/tests_harness/test_composition.py backend/tests_harness/modules/test_sop_definition.py` | 16/16 PASS: current StaffDeck owner supports sub-SOP definition metadata and cycle validation; adapter projects `subSopId` without inventing lifecycle entry/return behavior. |
| Exported SOP-disabled deployment boot | `pilotdeck-only.yaml` export, `docker compose --project-name pilotdeckdisabledverify up --detach` | PASS: Compose contained only `pilotdeck`; HTTP and Gateway started without any StaffDeck service. |
| Module profile validation | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build && PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/pilot/config/loadPilotConfig.spec.js dist/tests/pilot/config/modules-config.spec.js` | 25/25 PASS: absent modules, disabled SOP, disabled Skill, unsupported ownership, invalid endpoint/definition/default-id/timeout, malformed deployment settings, incomplete external bindings, and invalid AgentLoop transport fields fail or resolve before startup as specified. A disabled Skill binding clears session Skill contributions and `read_skill`; Gateway management calls fail as `SKILL_MODULE_DISABLED` without reaching native or remote Skill owners. A pure external AgentLoop remains permitted to use its host-owned native capabilities; when it is combined with any external host slot, every remaining core slot must be explicitly native or external, so it cannot silently fall back. The valid counterpart, with external AgentLoop/Model and explicit native Skill/Tool/Context/Knowledge bindings, is also covered. An explicit unsupported `stateMode` is rejected as `MODULE_STATE_MODE_UNSUPPORTED` for both external core modules and SOP rather than being silently accepted as an unknown field. A rejected module-profile reload retains the active configuration snapshot, publishes no partial replacement, and retains the reload diagnostic. |
| PLUG-03 optional capability profiles | `npm run build`; focused modules/config, external-session, exporter, SOP dependency, and protocol runners | PASS for the declared PLUG-03/04/05 matrix: disabled Skill/Knowledge bindings expose no calls or fallback, Knowledge-only/SOP-only/plain profiles retain declared dependencies, required SOP tools reject before owner execution, rejected reloads retain the active runtime, and the full timeout/disconnect/cancel protocol slice is recorded in `PARALLEL_PROTOCOL_REPORT.md`. This closes the declared G2 protocol matrix; it does not close integrated G4/G5. |
| A-package composition semantic regressions | `PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH npm run build && PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node --test --test-force-exit dist/tests/composition/runtime-ports.spec.js dist/tests/agent/loop/agent-status-projection.spec.js dist/tests/agent/modules/sidecar-client.spec.js`; `node products/pilotdeck-staffdeck-sop/conformance/run-compose-tool-ports-b0-differential.mjs`; `node products/pilotdeck-staffdeck-sop/conformance/run-e2e01-native-pilotdeck-b0-replay.mjs <trace>` | PARTIAL PASS: all reopened defects are closed. ToolPort scheduling independently matches frozen B0 for all-safe, all-non-safe, and mixed batches, including the phase barrier and original result slots; an injected non-safe ordering mismatch reports `FAIL`/exit `1`. The E2E B0 Skill replay compares saved `read_skill` request parameters plus `content` and `raw.content` against B0 `SkillManager`/`createReadSkillTool`; a changed saved result fails through the same comparator. Native and direct-sidecar AgentLoop composition now expose the frozen B0 public status shape (`type`, `sessionId`, `turnId`, `event`, `detail`); sidecar-local failure severity remains internal to the host callback. No G0-G5 gate changes. |
| Mounted profile restart | shared-volume Compose project `profileflipverify`: disabled -> enabled -> disabled | PASS: the mounted YAML took effect after each restart; `sop-runtime` appeared only for the enabled profile and became healthy. `auth.db` remained in the reused volume. |
| SOP wait UI race guards | `npm --prefix ui test -- --run src/components/chat-v2/SopWaitBanner.test.tsx` | 4/4 PASS: double-click submits one resume request; delayed status from a prior session cannot replace the current banner. |
| Browser stale-tab handoff | local mock-provider deployment, two tabs on one handoff session | PASS: one tab accepted and prepared the continuation; the other tab's stale acceptance was rejected with no second continuation. |
| Browser interruption and profile behavior | `run-local-web-smoke.sh` with the deterministic local model/SOP adapter | PASS: desktop and 390x844 complete `read_file -> handoff -> refresh -> continuation -> terminal`; an offline status request shows `Failed to fetch` and reconnect preserves the same wait; waiting/completed session switching does not leak a banner; `PILOTDECK_WEB_SMOKE_SOP_ENABLED=false` completes ordinary `read_file` chat without SOP control UI. |
| Real-model tool/SOP workflow | local PilotDeck home + real `provider1/qwen3.6-flash-distill` + original StaffDeck adapter | PASS: real `read_file -> handoff -> host resume -> completed`; sanitized Gateway event summary contains successful `tool_call_finished` for `read_file`, and persisted SOP state is `completed`. |
| Current-candidate real-model smoke | `REAL_MODEL_SOURCE_PILOT_HOME=/Users/a1/.pilotdeck STAFFDECK_SOP_SMOKE_ENDPOINT=http://127.0.0.1:18193 REAL_MODEL_SMOKE_MODEL=provider1/qwen3.6-flash-distill PATH=/Users/a1/.nvm/versions/node/v22.23.1/bin:$PATH node products/pilotdeck-staffdeck-sop/scripts/run-real-model-smoke.mjs` | PASS: current candidate completed real `read_file -> handoff -> resume -> completed` against an isolated StaffDeck SOP process; both turns emitted successful `read_file` events and persisted `completed`. Runtime credentials were not copied into the artifact. The final native-five Knowledge/Compaction workflow is recorded separately in the durable G5 artifact. |
| Exported real-model Compose workflow | fresh `operator_approval` export, Docker Compose, `provider1/qwen3.6-flash-distill`, browser | PASS for deployment/restart/recovery: real-model handoff survived independent SOP and PilotDeck restarts; browser refresh restored the same wait and UI resume returned it to the ordinary AgentLoop path, where the real model completed the SOP. A stopped SOP sidecar returned actionable `StaffDeck SOP runtime request failed: fetch failed` without state advance; the same persisted session completed after recovery. The shipped definition declares no `lookup_account` binding. Exported UI single-reply coverage remains listed in `REMAINING_TEST_PLAN.md`. |
| Exported browser UI smoke | `run-exported-web-smoke.mjs` using the local deterministic OpenAI-compatible model | PASS: a fresh export built and ran in Compose. At 1280x900 and 390x844, browser handoff survived refresh, UI resume prepared the ordinary composer turn, and exactly one terminal reply was rendered without a residual wait banner. A stale second tab could not prepare another continuation; a completed and waiting session did not leak banners; exported screenshots were retained and the test project, network, and volume were removed. |
| Exported SOP-disabled browser smoke | `PILOTDECK_EXPORTED_WEB_SOP_ENABLED=false run-exported-web-smoke.mjs` | PASS: the export contained only PilotDeck. At 1280x900 and 390x844, the browser explicitly selected Full Access, executed the ordinary PilotDeck `read_file` tool turn, rendered one terminal reply, and never rendered the SOP wait banner. |
| Final native-five real-provider workflow | `conformance/artifacts/g5-native-five-continuous-20260920/evidence-summary.json` and `transcript.sanitized.jsonl` | PASS: one final-artifact session records successful `read_skill`, `read_file`, `knowledge_query` with 4 evidence chunks and 4 citations, SOP handoff with a persisted wait ID, accepted resume, completed `submit_step_result`, and a durable compaction boundary with `summaryGenerated: true`. Runtime credentials and provider URLs were not retained; earlier failed samples remain preserved separately. |
| Malformed profile rejection | temporary malformed `pilotdeck.yaml` via `loadPilotConfig` | PASS: rejected before startup with `CONFIG_YAML_INVALID`; no runtime was created. |

Compose `config` validates manifest parsing only. The mock-browser and local
real-model checks are intentionally complementary to the exported generic SOP
deployment: the default export does not include account onboarding or
`lookup_account`. This closes LIVE-01 for the declared profile. SEM-01 remains
the direct v3 owner-versus-glue matrix described in `REMAINING_TEST_PLAN.md`.

These results close the declared fixed SOP profile, all G0-G5 gates, the
delivered G2 protocol matrix, the stated Knowledge owner/facade matrix, the
CTX-01/CMP-01/CMP-02 cases, and the deterministic E2E-01..E2E-05/REC-01/REC-02
composition. G4 and G5 are backed by durable sanitized artifacts. The
authoritative G0-G5 table is in
[ACCEPTANCE_REQUIREMENTS.md](ACCEPTANCE_REQUIREMENTS.md).

`./products/pilotdeck-staffdeck-sop/scripts/run-local-e2e.sh` runs the first
three deterministic gates together against a real local StaffDeck HTTP
process.

The recovery suite covers:

- an ordinary PilotDeck tool failure followed by a successful retry in the
  same session;
- model stream interruption and explicit cancellation without SOP progress;
- recovery after a successful tool result but before SOP submission;
- recovery after SOP state commit but before reply durability;
- recovery after reply durability but before the terminal event, including a
  child-process `SIGKILL` before delivery-journal cleanup, with one visible
  reply after transcript replay and no second model/SOP invocation.
- persisted v1/v2 SOP waits and pending deliveries migrating to v3 without
  losing their revision, wait identity, slots, or delivery record;
- invalid JSON being rejected without silently overwriting it with a new SOP
  session.

The current browser rerun was executed against the current StaffDeck adapter,
not the removed copied runtime. It covers desktop/mobile refresh recovery,
disconnect/reconnect, session isolation, and the SOP-disabled profile. The
component suite and two-tab run cover synchronous double-click and stale-tab
resume rejection.

## Retired Historical Runner

Historical StaffDeck legacy-versus-sidecar parity is not a gate for this v3
composition. The requested
StaffDeck checkout at `/Users/a1/Desktop/claw/openbmb/StaffDeck-portable-sop`
tracks `codex/dsh-pluggable-runtime` but does not contain either required
entrypoint:

- `tools/agent-loop-parity/run.py`
- `tools/real-deployment-e2e.py`

That historical comparison remains unavailable without a ported baseline.
Original SOP module sources and tests are instead the owner oracle for the
current v3 module-contract comparison.

On 2026-09-17, a side-effect-free `pure_text` probe ran the preserved historical
runner against the current two checkouts. After its test-only import path was
made aware of the current `backend/src`, both historical adapter modes failed
before executing a turn because current `app.core.turn_coordinator` no longer
exports the runner's required `TurnPlanner` API. The runner reported two
`BLOCKED` adapters (exit `2`) and no semantic comparison. This confirms that
the runner is an adapter/API-porting task, not an untested deployment failure.

The target's actual engine routing confirms that this cannot be repaired by a
test-only import alias: `AgentLoop._open_engine()` always selects `EngineHost`,
and `EngineHost.open()` explicitly rejects `engine.harness_v2` as
`HARNESS_V2_RETIRED`. Only `engine.harness_v3` is a live execution owner. The
historical runner's legacy-vs-sidecar mode switch therefore has no valid pair
to execute here. Reinstating a legacy route merely for parity would change the
module-selection semantics being accepted; a replacement requires an approved
baseline and mapping rather than a fabricated second path.
# v3 Candidate Addendum (2026-09-17)

This addendum is the current candidate evidence. It is deliberately separate
from the historical entries below. Scope is the fixed composition: PilotDeck
AgentLoop, tools, and model provider plus the StaffDeck `operator_approval`
SOP. It does not deploy account onboarding, `lookup_account`, or unimplemented
sub-SOP execution.

| Gate | Command / result | Status |
| --- | --- | --- |
| PilotDeck protocol and loop regression | `npm run build`; `node --test --test-force-exit dist/tests/sop/staffdeck-sop-client.spec.js dist/tests/sop/staffdeck-sop-agent-loop.spec.js` | PASS, 25/25. Includes malformed operation payloads, legal owner `null` optional fields, timeout/cancel retryability, error projection, revision fencing, and no reply-journal mutation on rejection. |
| Portable SOP and independent owner oracle | `PYTHONPATH=portable_sop/src:backend:backend/src ... pytest -q portable_sop/tests` | PASS, 49/49. `test_owner_contract.py` invokes native `SopRuntimeModule.submission_validator()`, `build().after_execution()`, and `GraphRules`; it does not use portable `prepare`/`submit` to construct expected values. |
| StaffDeck SOP owner tests | `PYTHONPATH=backend:backend/src ... pytest -q backend/tests_harness/test_composition.py backend/tests_harness/modules/test_sop_definition.py` | PASS, 16/16. Sub-SOP coverage remains metadata/validation only because this owner has no child execution lifecycle. |
| Fresh enabled export, browser and recovery | `run-exported-web-smoke.mjs` with Chrome executable, artifacts `/tmp/pilotdeck-v3-enabled-artifacts/result.json` | PASS. 1280x900 and 390x844 handoff/refresh/UI resume/one terminal reply, stale-tab rejection and session isolation. While waiting, PilotDeck and `sop-runtime` were restarted and retained the wait id; stopping the sidecar rejected completion without state advance, then the same session completed after recovery. Screenshots are in that artifact directory. |
| Fresh disabled export, browser | `PILOTDECK_EXPORTED_WEB_SOP_ENABLED=false run-exported-web-smoke.mjs`, artifacts `/tmp/pilotdeck-v3-disabled-artifacts/result.json` | PASS. 1280x900 and 390x844 executed the ordinary `read_file` tool flow with no SOP service or wait banner. |
| Exported Compose real provider | `PILOTDECK_EXPORTED_WEB_REAL_MODEL=1 ... run-exported-web-smoke.mjs`; artifacts `/tmp/pilotdeck-v3-real-model-artifacts/result.json` and `real-model-completed.png` | PASS. A newly exported Compose deployment used `provider1/qwen3.6-flash-distill`; the browser submitted the initial turn, entered StaffDeck handoff, resumed through the SOP UI, and reached structured `completed` without relying on exact generated wording. The temporary YAML contained only `${PILOTDECK_REAL_MODEL_API_KEY}`; the credential was injected through the Compose process environment and was not printed or copied. Containers, network, and volume were removed. |

The sidecar receives `idempotencyKey` in the v2 envelope, but this candidate
does **not** claim durable server-side idempotency/deduplication was verified.
The owner comparison above is an independent v3 contract oracle; the older
adapter-direct-call comparison remains only a transport regression.
